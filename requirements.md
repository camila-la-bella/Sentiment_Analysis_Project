本项目依赖以下 Python 库，请在运行前确保已安装：

- `torch` — PyTorch 深度学习框架
- `numpy` — 科学计算库
- `scikit-learn` — 提供常用的机器学习指标（如 `accuracy_score`）
- `gensim` — 用于加载预训练词向量（如 `KeyedVectors`）
- `tqdm` — 显示进度条，提升训练可视性

推荐使用以下命令安装所有依赖：

```bash
pip install torch numpy scikit-learn gensim tqdm
```